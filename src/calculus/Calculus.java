package calculus;

import numbers.PNumber;
import numbers.Real;
import operations.Arithmetic;

public class Calculus {
	
	/* Attributes */
	
	//public static boolean USE_CALCULUS = true; // Configuration variable.
	
	// constantes ou funções? (usar scale...) - enum?
	// escala própria?
	// nomes melhores
	//public static final INFINITESIMAL_SCALE = 9999;
	//public static final Real INFINITY = Arithmetic.pow(10, Constants.SUPER_PRECISION_SCALE + 1).subt(1);
	//public static final Real INF_NEG = INFINITY.negate();
	//public static final Real ZERO_POS = Arithmetic.div(new Real(1), Arithmetic.pow(10, Constants.SUPER_PRECISION_SCALE), Constants.SUPER_PRECISION_SCALE);
	//public static final Real ZERO_NEG = ZERO_POS.negate();
	
	/* Methods */
	
	// lim(Symbol x, Real valueApproached, Function/String function) ?
	
	// series()
}
